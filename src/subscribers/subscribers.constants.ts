/** Watchlist assigned to a new subscriber when `/dangky` is called with no symbols. */
export const DEFAULT_WATCHLIST = ['btc', 'eth'];

/** Hard cap on symbols per subscriber watchlist, to keep digest sends bounded. */
export const MAX_WATCHLIST_SIZE = 20;
