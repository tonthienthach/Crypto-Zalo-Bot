# EPIC-001 — Multi-tenant digest subscriptions

Initiative 1 (docs/ROADMAP.md): replace the single hardcoded DIGEST_CHAT_ID/DIGEST_COIN_SYMBOLS recipient with a Postgres-backed subscriber list (/dangky, /huy, /watchlist), so any chat can manage its own daily digest subscription and watchlist.
