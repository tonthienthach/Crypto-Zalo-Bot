# compiled output
/dist
/node_modules
/build
/.vercel

# aidlc — shared team process config (workspace.yaml, skills/, templates/,
# runs/) is tracked so every dev/agent gets the same pipeline; see
# docs/ROADMAP.md and docs/epics/. Only genuinely local artifact scratch
# state (docs/epics/*/state.json aside — that one IS tracked) would go here
# if it ever needs excluding again.

# logs
logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# env
.env
.env.local
.env.*.local

# coverage
/coverage
/.nyc_output

# IDE
.idea
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
.DS_Store

# ngrok
ngrok.yml

.vercel
.env*
